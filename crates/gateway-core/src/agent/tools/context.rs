//! Tool Execution Context
//!
//! Re-exported from `gateway-tool-types`. The base `ToolContext` is portable
//! and does not depend on gateway-core types like `HookExecutor`.

pub use gateway_tool_types::ToolContext;
